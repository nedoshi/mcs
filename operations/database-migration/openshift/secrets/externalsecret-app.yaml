apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: db-credentials-app
  namespace: db-migration
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore
  target:
    name: db-credentials-app
    creationPolicy: Owner
  data:
    - secretKey: PGUSER
      remoteRef:
        key: db-migration/app
        property: username
    - secretKey: PGPASSWORD
      remoteRef:
        key: db-migration/app
        property: password
    - secretKey: PGHOST
      remoteRef:
        key: db-migration/app
        property: host
    - secretKey: PGPORT
      remoteRef:
        key: db-migration/app
        property: port
    - secretKey: PGDATABASE
      remoteRef:
        key: db-migration/app
        property: dbname
