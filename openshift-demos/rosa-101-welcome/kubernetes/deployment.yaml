---
apiVersion: v1
kind: Namespace
metadata:
  name: rosa-demo
  labels:
    app: rosa-demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rosa-demo
  namespace: rosa-demo
  labels:
    app: rosa-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rosa-demo
  template:
    metadata:
      labels:
        app: rosa-demo
    spec:
      containers:
        - name: rosa-demo
          # Use only after a successful build. Import-from-git creates this automatically.
          image: image-registry.openshift-image-registry.svc:5000/rosa-demo/rosa-demo:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: WELCOME_MESSAGE
              value: "Welcome to ROSA!"
            - name: APP_VERSION
              value: "1.0.0"
            - name: OPENSHIFT_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: rosa-demo
  namespace: rosa-demo
  labels:
    app: rosa-demo
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: rosa-demo
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rosa-demo
  namespace: rosa-demo
  labels:
    app: rosa-demo
spec:
  to:
    kind: Service
    name: rosa-demo
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
