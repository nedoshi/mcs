# MCS

Managed OpenShift (ROSA, ARO, OSD) and related projects.

**Documentation:** [docs/README.md](docs/README.md)

Top-level areas: `cluster-creation-cloud`, `cluster-creation-onprem`, `docs`, `examples`, `install-operators`, `networking`, `openshift-demos`, `operations`, `homelab`. Each has its own README where needed — avoid duplicating links here.
