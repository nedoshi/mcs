# Deprecated

Networking is provided by the Red Hat module `terraform-redhat/rosa-hcp/rhcs//modules/vpc` in the root `network.tf`.

This vendored copy is retained for reference only and is not used by `terraform apply`.
